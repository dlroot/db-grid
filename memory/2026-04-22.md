# 2026-04-22 - db-grid 调试与测试完善

## 时段 13:40 - 15:54

## 对齐 Bug 调试（继续）
从 04:52 开始的对齐问题延续至今。

### 修复历程
1. `092fda0` — 主题 SCSS 未被 styles.scss 导入（根因）
2. `8ddf654` — 排序点击后 header 不刷新
3. `a4b24de` — 表头对齐：CellRendererService 用 ColumnService 统一宽度
4. `18ba26d` — cell-renderer 直接用 colDef.width（绕过 columnService）
5. `2b12c07` — virtualScroll/rowsContainer 设置总列宽；onScroll 同步 scrollLeft 到 header

### 当前状态
- cell-renderer 宽度：`let i = e.width || 200`
- header-cell 宽度：`state?.width || colDef.width || 200`
- header-container: `overflow-x: auto`
- onScroll 同步 scrollLeft

**问题未解决**，需要用户本地验证

## 单元测试完善

### 新增测试文件
- `column.service.spec.ts` — 30 个测试（initialize/flatten/colId/width/hidden/pinned/totalWidth/reset/groups/depth）
- `selection.service.spec.ts` — 24 个测试（mode/select/deselect/clear/selectAll/Ctrl+click/Shift/IDs/data）

### 已知 Bug（测试发现但未修）
- `SelectionService.isEverythingSelected()` 只检查 `node.selected` 属性，不检查 Map 状态，导致不同 node 实例判断错误

### 测试结果
```
5 test files, 138 tests passing
- data.service.spec.ts: 84
- editor.service.spec.ts: (existing)
- filter.service.spec.ts: (existing)
- column.service.spec.ts: 30 (new)
- selection.service.spec.ts: 23 (new, 1 调整)
```

### Vitest 注意事项
- 不要用 `jasmine.createSpy()` → 用 `vi.fn()`
- 不要用 `jasmine.objectContaining()` → 用 `expect().toBe()` 或其他 Vitest matchers
- `jasmine` reference 在 vitest 环境中不存在

## Git
- master: 2b12c07 (fix + test)
- GitHub Pages: https://dlroot.github.io/db-grid/

## 待做
- [ ] 对齐 Bug 根因（用户未确认）
- [ ] Grid 初始渲染数据空（ngAfterViewInit 时序问题）
- [ ] SelectionService.isEverythingSelected() bug 修复
- [ ] 覆盖率提升（还有 ~27 个 service 无测试）
